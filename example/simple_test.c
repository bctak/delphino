#include <stdio.h>

int main(void)
{
    int num = 0;
    scanf("%d",&num);
    if(num == 0)
    {
        printf("num is zero");
    }
    else if(num == 1)
    {
        num = 0;
    }
    getpid();

    return 0;
}